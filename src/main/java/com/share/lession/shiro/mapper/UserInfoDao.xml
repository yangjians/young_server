<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.share.lession.shiro.dao.UserInfoDao">

    <!-- 开启二级缓存 -->
    <!--<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>-->

    <select id="findByUserName" resultType="com.share.lession.shiro.vo.UserInfo">
        select id uuid,state, username ,password,username name from young.user_info where username=#{username}
    </select>
    <select id="selectByMap" resultType="com.share.lession.shiro.vo.SysUser" parameterType="Map">
        select username ,password,to_char(state) status from young.user_info where username=#{username} and password=#{pswd}
    </select>
</mapper>
